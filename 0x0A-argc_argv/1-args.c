#include "holberton.h"
#include <stdio.h>
/**
 * main - this program prints the number of elements
(* a blank line
*@argc: this is the quantity of parameters
*@argv: the parameter to put in the program.
* Description: this program prints the number of elements)?
* Return: return 0 or 1
 */
int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d\n", argc - 1);
	return (0);
}
